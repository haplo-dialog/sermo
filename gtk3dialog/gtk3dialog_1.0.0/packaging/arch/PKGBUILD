# Maintainer: haplo-dialog <devel@haplo-dialog.fr>
# Port: GTK3 — haplo-dialog family

pkgname=haplo-dialog
_srcname=gtk3dialog   # nom de l'archive amont, inchange
pkgver=1.0.0
pkgrel=1
epoch=
pkgdesc="GTK3 dialog window creator for shell scripts"
arch=('x86_64' 'aarch64')
url="https://haplo-dialog.fr"
license=('GPL-2.0-or-later')
groups=()
depends=('gtk3' 'vte3' 'gtk-layer-shell')
makedepends=('gtk3' 'vte3' 'gtk-layer-shell' 'flex' 'bison' 'autoconf' 'automake' 'pkgconfig')
checkdepends=()
optdepends=()
provides=('gtk3dialog' 'gtkdialog')
conflicts=('gtk3dialog')
replaces=('gtk3dialog')
backup=()
options=()
install=
changelog=
source=("$_srcname-$pkgver.tar.gz::https://haplo-dialog.fr/pub/gtk3dialog/$_srcname-$pkgver.tar.gz")
noextract=()
sha256sums=('SKIP')

prepare() {
  cd "$_srcname-$pkgver"
  # Patch de compatibilité si besoin
  :
}

build() {
  cd "$_srcname-$pkgver"
  NOCONFIGURE=1 ./autogen.sh
  ./configure --prefix=/usr \
              --mandir=/usr/share/man \
              
  make
}

check() {
  cd "$_srcname-$pkgver"
  # make check 2>/dev/null || true
  :
}

package() {
  cd "$_srcname-$pkgver"
  make DESTDIR="$pkgdir" install
  install -Dm644 COPYING "$pkgdir/usr/share/licenses/$pkgname/COPYING"
  install -d "$pkgdir/usr/share/doc/$pkgname/examples"
  cp -r examples/* "$pkgdir/usr/share/doc/$pkgname/examples/" 2>/dev/null || true
}
